Import('context')

env = Environment()

env.Append(CXXFLAGS = '-std=c++11')

env.Append(CPPDEFINES = ['BOOST_TEST_DYN_LINK', 'CARSTAIRS_TEST'])

env.Program(
    'testcarstairs',
    context.sources(),
    LIBS = ['fftw3', 'python3.4', 'boost_filesystem', 'boost_system', 'boost_unit_test_framework', 'pthread'],
    LIBPATH = '/usr/lib/python3.4/config-3.4m-x86_64-linux-gnu',
)
